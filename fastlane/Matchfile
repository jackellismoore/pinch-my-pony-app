# Match storage repo (NO credentials in this URL)
git_url(ENV["MATCH_GIT_URL"])

storage_mode("git")
type("appstore")
app_identifier(["com.pinchmypony.app"])

# use main unless you intentionally chose something else
git_branch("main")

# avoid any prompts in CI
readonly(false)
