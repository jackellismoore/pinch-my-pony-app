# frozen_string_literal: true

opt_out_usage
default_platform(:ios)

platform :ios do
  desc "No-op placeholder"
  lane :upload_only do
    UI.message("Upload is handled by xcrun altool in GitHub Actions.")
  end
end