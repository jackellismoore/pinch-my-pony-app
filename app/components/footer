export default function Footer() {
  return (
    <footer
      style={{
        marginTop: 80,
        padding: "32px 24px",
        borderTop: "1px solid #e5e5e5",
        textAlign: "center",
        fontSize: 14,
        color: "#666",
      }}
    >
      <p style={{ marginBottom: 8 }}>
        🐴 Pinch My Pony — built with care for horses and humans
      </p>

      <p>
        Owners stay in control • Borrowers are verified • Safety first
      </p>

      <p style={{ marginTop: 12, fontSize: 12 }}>
        © {new Date().getFullYear()} Pinch My Pony — Demo UI
      </p>
    </footer>
  );
}
